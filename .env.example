# Copy to .env and adjust. docker compose reads this automatically.
# EDGEDEPTH_WS_URL is consumed by the browser, so it must be host-reachable.
EDGEDEPTH_WS_URL=ws://localhost:9001/ws
EDGEDEPTH_LOG=info
